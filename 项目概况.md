# CloudBox 项目概况

CloudBox 是一个逐步演进的 Go 文件存储学习项目。当前版本不是分布式网盘成品，而是一个已经可运行、可测试的本地文件存储后端。

## 当前能力

```text
用户认证
  └── JWT 鉴权

文件管理
  ├── 小文件上传和下载
  ├── Range 下载
  ├── 软删除、回收站和恢复
  ├── 文件重命名和移动
  ├── 多层文件夹：创建、浏览、重命名、移动和安全删除
  └── 按用户隔离

内容存储
  ├── SHA-256 内容去重
  ├── 秒传
  └── 按用户逻辑用量统计与 1 GiB 配额限制

大文件上传
  ├── 创建上传任务
  ├── 分片乱序和重复上传
  ├── 查询已完成分片
  ├── 分片和完整文件哈希校验
  └── 合并为正式文件并落入指定文件夹

分享链接
  ├── 创建随机 token 和可选 bcrypt 密码
  ├── 过期时间与下载次数限制
  ├── 公开 Range 下载
  └── 查看和撤销自己的分享链接
```

## 已完成目标

| 阶段 | 目标 | 状态 |
| --- | --- | --- |
| 基础 API | Gin、SQLite、JWT、分层结构 | 已完成 |
| 文件管理 | 上传、列表、下载、回收站 | 已完成 |
| 内容去重 | `file_objects`、SHA-256、秒传 | 已完成 |
| 大文件传输 | Range 下载、分片上传、断点续传基础 | 已完成 |
| 并发保护 | 条件状态转换，单次完成只创建一个文件 | 已完成 |
| 资源回收 | 取消上传并删除临时分片 | 已完成 |
| 自动清理 | 启动时与每小时清理 24 小时未活动的上传任务 | 已完成 |
| 文件夹管理 | 文件夹树、文件和文件夹移动、重命名、空目录删除 | 已完成 |
| 存储配额 | 使用量查询、普通上传、秒传和分片上传限制 | 已完成 |
| 分享链接 | 密码、过期、下载上限、公开下载与主动撤销 | 已完成 |
| 测试 | Repository、Service、Handler、端到端手工验证 | 已完成 |

## 未完成目标

| 优先级 | 目标 | 学习重点 |
| --- | --- | --- |
| 中 | MinIO、PostgreSQL、Redis | 基础设施替换和缓存 |
| 低 | 异步缩略图和失败重试 | 队列和后台任务 |
| 低 | Docker、CI、监控和追踪 | 工程化和可观测性 |
| 低 | Web 前端 | 浏览器端分片、进度展示 |

## 当前上传任务状态

```text
uploading
    |
    | 所有分片存在且校验通过
    v
completing
    |
    | 合并、校验完整哈希、创建正式文件
    v
completed

失败时：恢复为 uploading，保留分片供客户端修复或重试。
```

## 当前边界

- 存储后端是本地磁盘，不是 MinIO 或 S3。
- 数据库是 SQLite，适合学习和单机开发。
- 分片状态保存在 SQLite，尚未使用 Redis。
- 已支持重试上传相同分片，并通过条件状态转换避免重复完成。
- 过期任务默认以 24 小时为界，服务启动时及每小时自动清理一次。

详细接口和启动方式见 [README.md](README.md)。历史设计与阶段计划保留在 `docs/`。
