## CloudBox 分布式文件存储与分享平台

可以理解为一个简化版的百度网盘、Dropbox 或阿里云盘。用户可以上传、下载、预览、分享和管理文件，系统支持大文件分片、断点续传、秒传、并发下载和异步处理。

这个项目比普通商城、博客、学生管理系统更能体现 Go 的优势，因为会大量使用网络编程、文件流处理、并发控制和异步任务。

------

## 一、核心业务流程

用户上传一个 2GB 文件时：

```
选择文件
   ↓
计算文件哈希
   ↓
检查服务器是否已存在
   ├─ 已存在：直接秒传
   └─ 不存在：文件分片
          ↓
     并发上传分片
          ↓
     服务端校验并合并
          ↓
     上传至对象存储
          ↓
     异步生成缩略图
          ↓
     保存文件元数据
```

最终用户可以：

- 创建文件夹；
- 上传和下载文件；
- 查看上传进度；
- 暂停后继续上传；
- 生成有期限的分享链接；
- 设置分享密码；
- 回收站恢复文件；
- 查看存储空间用量；
- 预览图片、文本和 PDF。

------

## 二、推荐技术栈

后端以 Go 为核心：

- Go
- Gin 或 Chi
- PostgreSQL
- Redis
- MinIO
- Redis Streams
- JWT
- SSE
- Docker Compose
- Prometheus
- OpenTelemetry
- React 或 Vue

Go 的 `io.Reader`、`io.Writer` 和 `io.CopyBuffer` 很适合流式文件传输，避免一次性将大文件全部加载进内存；`net/http` 可以直接实现上传下载接口；`context` 可以处理上传取消、请求超时和客户端断开连接。

------

## 三、最重要的功能

### 1. 大文件分片上传

前端将大文件分成若干块：

```
2GB 文件
├── chunk-001  10MB
├── chunk-002  10MB
├── chunk-003  10MB
└── ...
```

接口设计：

```
POST /api/uploads/init
POST /api/uploads/{uploadID}/chunks/{chunkNumber}
GET  /api/uploads/{uploadID}/status
POST /api/uploads/{uploadID}/complete
```

后端需要处理：

- 分片乱序到达；
- 重复上传同一分片；
- 分片校验失败；
- 上传中断；
- 合并过程中服务退出；
- 多个请求同时触发合并。

这部分可以学习：

- goroutine；
- channel；
- mutex；
- `sync.WaitGroup`；
- 文件流；
- 并发安全；
- `context.Context`。

------

### 2. 断点续传

数据库或 Redis 中保存已经完成的分片编号：

```
{
  "upload_id": "up_123456",
  "completed_chunks": [1, 2, 3, 5, 6],
  "missing_chunks": [4, 7, 8]
}
```

网络中断后，客户端只需要重新上传缺失分片，不需要从头开始。

面试中可以重点讲：

- 上传状态放 Redis 还是数据库；
- Redis 数据丢失后如何恢复；
- 如何清理长期未完成的上传；
- 如何保证分片状态与对象存储一致。

------

### 3. 文件秒传和去重

上传前计算 SHA-256：

```
用户上传文件
     ↓
计算 SHA-256
     ↓
查询 file_object 表
     ├─ 存在：只创建用户文件记录
     └─ 不存在：真正上传文件内容
```

需要区分两个概念：

```
file_object：实际存储对象
user_file：用户看到的文件记录
```

多个用户上传相同文件时，只保存一份物理数据，但每个用户拥有独立的文件名称、目录和权限。

为了避免两个请求同时上传相同文件，可以使用：

- 数据库唯一索引；
- Redis 分布式锁；
- 数据库事务；
- 条件更新。

------

### 4. 并发下载与 HTTP Range

下载接口：

```
GET /api/files/{fileID}/download
```

支持：

```
Range: bytes=1048576-2097151
```

这样用户可以：

- 暂停后继续下载；
- 使用多线程下载；
- 拖动视频播放进度；
- 只请求文件的一部分。

Go 的 HTTP 服务和流式 I/O 接口适合实现这类不落内存的大文件传输。

------

### 5. 分享链接

用户可以创建：

```
{
  "file_id": "file_123",
  "password": "8692",
  "expires_in": 86400,
  "download_limit": 10
}
```

需要实现：

- 随机分享码；
- 密码校验；
- 有效期；
- 下载次数限制；
- 分享取消；
- 防止暴力破解；
- 防止越权访问。

这是展示权限设计和 Redis 使用能力的好模块。

------

### 6. 异步文件处理

上传完成后发送事件：

```
file.uploaded
```

消费者异步执行：

- 图片缩略图生成；
- 文件类型识别；
- 病毒扫描；
- 视频元数据提取；
- 文本内容索引；
- 清理失败分片；
- 存储空间统计。

Redis Streams 支持消费者组、消息确认和待处理消息管理，可用于实现这些异步任务。

------

## 四、数据库核心表

建议至少包含：

```
users
file_objects
user_files
folders
upload_tasks
upload_chunks
share_links
download_records
storage_quotas
operation_logs
```

其中最关键的是：

```
file_objects
------------
id
file_hash
storage_key
file_size
mime_type
reference_count
created_at
user_files
----------
id
user_id
object_id
parent_folder_id
file_name
status
created_at
deleted_at
```

物理文件和用户文件记录分离后，才能正确支持秒传、去重、回收站和引用计数。

------

## 五、开发阶段

### 第一阶段：基础版

先完成：

- 用户注册和登录；
- 文件夹管理；
- 小文件上传；
- 文件下载；
- MinIO 存储；
- 文件列表；
- 删除和回收站；
- Docker Compose 部署。

这一阶段主要学习 Go Web 开发、数据库操作和基础工程结构。

### 第二阶段：简历可用版

增加：

- 大文件分片上传；
- 断点续传；
- 文件秒传；
- SHA-256 校验；
- 并发上传；
- Range 下载；
- 分享链接；
- 存储配额；
- Redis 缓存。

完成这一阶段后就可以作为秋招项目。

### 第三阶段：项目亮点版

继续增加：

- Redis Streams 异步任务；
- 失败任务重试；
- 上传任务过期清理；
- 限流；
- 接口幂等；
- OpenTelemetry；
- Prometheus 监控；
- 压力测试；
- GitHub Actions；
- Kubernetes 部署。

OpenTelemetry 的 Go 实现可以采集 traces、metrics 和 logs，用于分析上传接口、数据库、Redis 和对象存储之间的调用链。

------

## 六、建议的项目结构

```
cloudbox/
├── cmd/
│   ├── api/
│   │   └── main.go
│   └── worker/
│       └── main.go
├── internal/
│   ├── auth/
│   ├── config/
│   ├── file/
│   ├── folder/
│   ├── upload/
│   ├── download/
│   ├── share/
│   ├── storage/
│   ├── queue/
│   ├── middleware/
│   └── repository/
├── migrations/
├── deployments/
├── tests/
├── docker-compose.yml
├── Makefile
└── README.md
```

前期不要拆成大量微服务。可以只运行两个 Go 进程：

```
cloudbox-api
cloudbox-worker
```

------

## 七、简历描述参考

### 项目名称

**CloudBox——基于 Go 的分布式文件存储与分享平台**

### 项目介绍

基于 Go 开发面向大文件场景的文件存储与分享平台，支持分片上传、断点续传、文件秒传、并发下载、分享链接、异步文件处理及存储配额管理。

### 技术亮点

- 基于 Go `io.Reader` 和流式传输实现大文件上传下载，避免文件整体加载至内存，降低服务内存占用。
- 设计分片上传协议，支持分片并发上传、断点续传、完整性校验和失败分片重传。
- 根据文件 SHA-256 建立内容寻址机制，通过文件对象与用户文件记录分离实现文件秒传和存储去重。
- 使用数据库唯一索引、事务及 Redis 锁控制文件合并流程，避免并发请求造成重复合并和重复存储。
- 基于 HTTP Range 实现断点下载和多线程下载，支持客户端恢复中断的下载任务。
- 使用 Redis Streams 实现缩略图生成、垃圾文件清理和存储统计等异步任务，并支持消息确认和失败重试。
- 接入 Prometheus 和 OpenTelemetry，监控上传吞吐量、接口耗时、任务积压及对象存储调用链。