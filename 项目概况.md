# CloudBox 项目概况

CloudBox 是一个用 Go 实现的文件存储后端学习项目。当前版本提供可运行、可测试的单体 API 服务，支持本地磁盘或 MinIO 对象存储，以及 SQLite 或 PostgreSQL 数据库。

本文说明当前后端能力和前端设计边界。接口命令、环境变量和部署步骤以 [README.md](README.md) 为准。

## 当前定位

- 项目类型：面向学习的网盘后端，不是分布式网盘成品。
- 服务形态：单个 Go API 进程，内置持久化后台 Worker。
- 客户端状态：尚未实现 Web 前端。
- 默认运行模式：SQLite、本地磁盘存储、Redis 和 ClamAV 关闭。

## 已完成能力

| 模块 | 当前能力 |
| --- | --- |
| 认证 | 注册、bcrypt 密码哈希、JWT 登录和受保护路由鉴权。 |
| 文件管理 | 小文件上传、文件列表、下载、HTTP Range、软删除、回收站、恢复、永久删除、重命名和移动。 |
| 文件夹 | 创建、浏览、重命名、移动和仅删除空文件夹。 |
| 内容存储 | SHA-256 内容去重、秒传、`file_objects` 与 `user_files` 分离、按用户逻辑配额统计。 |
| 分片上传 | 初始化、乱序上传、覆盖重复分片、上传进度、分片与整文件校验、合并和取消。 |
| 分享 | 随机 token、可选密码、过期时间、下载次数上限、公开 Range 下载和撤销。 |
| 后台任务 | 持久化队列、原子领取、重试、文件完整性校验、缩略图和病毒扫描。 |
| 图片预览 | JPEG、PNG、GIF 的 PNG 缩略图，最长边 `320 px`。 |
| 病毒扫描 | 可选 ClamAV 流式扫描。启用后，只有扫描状态为 `clean` 的对象可下载、读取缩略图或通过分享链接访问。 |
| 基础设施 | 本地磁盘或 MinIO、SQLite 或 PostgreSQL、可选 Redis 用量缓存、Docker Compose、GitHub Actions。 |
| 可观测性 | JSON 访问日志、请求 ID、Prometheus HTTP 指标和 OpenTelemetry HTTP 链路追踪。 |

## 关键模型和状态

### 用户文件与物理对象

`user_files` 表示用户可见的文件记录。`file_objects` 表示实际存储对象。多个内容相同的用户文件可以指向同一个 `file_object`，因此共享文件字节、缩略图和病毒扫描结果。

用户文件状态：

```text
active   可在文件列表和下载接口中访问
deleted  位于回收站；可恢复或永久删除
```

### 分片上传

```text
uploading
    |
    | 分片完整且校验通过
    v
completing
    |
    | 合并、校验哈希、创建正式文件
    v
completed
```

合并失败时，上传任务恢复为 `uploading`，已上传分片保留，客户端可以修复分片后再次完成。

### 后台任务

后台任务状态：

```text
queued -> running -> succeeded
                 -> failed
```

失败任务按指数退避重新排队。达到任务最大尝试次数后，任务状态为 `failed`。

已实现任务类型：

- `file.verify`：重新计算 SHA-256，校验文件内容是否与数据库中的哈希一致。
- `file.thumbnail`：生成图片缩略图。
- `file.scan`：调用 ClamAV 检查文件内容。

### 病毒扫描

病毒扫描按 `file_object` 保存状态：

```text
pending -> scanning -> clean
                    -> infected
                    -> failed
```

启用 `CLAMAV_ENABLED=true` 后：

- `clean`：允许私有下载、缩略图读取和公开分享下载。
- `pending`、`scanning`、`failed` 或无扫描记录：下载路径返回 `423 Locked`。
- `infected`：私有下载和缩略图返回 `403 Forbidden`；公开分享返回 `423 Locked`，不暴露扫描详情。

图片文件在 `clean` 前不创建缩略图任务。

## 前端可使用的接口

除认证接口和公开分享下载外，`/api` 路由需要：

```text
Authorization: Bearer <JWT>
```

| 使用场景 | 接口 |
| --- | --- |
| 注册和登录 | `POST /api/auth/register`、`POST /api/auth/login` |
| 当前用户 | `GET /api/me` |
| 上传和秒传 | `POST /api/files`、`POST /api/files/instant` |
| 文件浏览 | `GET /api/files`、`GET /api/files?parent_id=<目录 ID>`、`GET /api/files/trash` |
| 文件操作 | `GET /api/files/:id/download`、`DELETE /api/files/:id`、`DELETE /api/files/:id/permanent`、`POST /api/files/:id/restore`、`PATCH /api/files/:id/move`、`PATCH /api/files/:id/rename` |
| 缩略图 | `GET /api/files/:id/thumbnail` |
| 文件夹 | `POST /api/folders`、`GET /api/folders`、`PATCH /api/folders/:id/rename`、`PATCH /api/folders/:id/move`、`DELETE /api/folders/:id` |
| 分片上传 | `POST /api/uploads/init`、`PUT /api/uploads/:id/chunks/:number`、`GET /api/uploads/:id`、`POST /api/uploads/:id/complete`、`DELETE /api/uploads/:id` |
| 文件完整性校验 | `POST /api/files/:id/verify`、`GET /api/jobs/:id` |
| 分享 | `POST /api/files/:id/shares`、`GET /api/shares`、`DELETE /api/shares/:token`、`GET /api/shares/:token/download` |
| 存储用量 | `GET /api/storage` |

## 前端设计边界

前端可以实现认证、文件浏览、目录操作、上传、分片上传、回收站、分享链接管理、存储用量和缩略图展示。

当前后端没有提供以下专用接口：

- 文件的病毒扫描状态查询接口。
- 手动重新触发病毒扫描的接口。
- 文件搜索、排序参数、分页和批量操作接口。
- 用户资料、密码修改和刷新令牌接口。

前端设计应把这些能力标记为待接入，不应假设接口已经存在。文件下载或缩略图请求返回 `423` 或 `403` 时，前端需要按当前 HTTP 状态显示不可用结果。

## 后续目标

| 优先级 | 目标 | 说明 |
| --- | --- | --- |
| 高 | Web 前端 | 基于现有 API 实现登录、文件浏览、上传和分享管理。 |
| 中 | 文件扫描状态 API | 让前端可展示 `pending`、`scanning`、`clean`、`infected` 和 `failed`。 |
| 中 | 上传体验 | 增加文件搜索、排序、分页、批量操作和更完整的上传进度展示。 |
| 低 | 部署扩展 | 根据实际需求补充更完整的生产部署说明。 |

## 验证状态

当前代码已通过：

```bash
/usr/local/go/bin/go test ./...
/usr/local/go/bin/go vet ./...
```

ClamAV 集成测试默认跳过。设置 `CLAMAV_TEST_ADDRESS` 后，测试会连接真实 clamd 服务：

```bash
CLAMAV_TEST_ADDRESS=127.0.0.1:3310 \
  /usr/local/go/bin/go test -count=1 -run TestClamAVScannerIntegration -v ./internal/scanner
```
